import { ChatInterface } from '@/components/chat/ChatInterface';

export const metadata = {
  title: 'Talk it through — Steady',
};

export default function ChatPage() {
  return <ChatInterface />;
}
